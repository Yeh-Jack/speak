==========   Backend   ==========
cd /workspaces/education/backend

# 0. Install uv and initiate virtual environment.
curl -LsSf https://astral.sh/uv/install.sh | sh
uv venv
uv pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu125

# 1. Run tests.
uv run pytest tests/unit/ -v

# 2. Start the backend server.
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8080

# 3. Get video information
curl -X POST http://localhost:8080/api/v1/videos/info \
  -H "Content-Type: application/json" \
  -d '{"youtube_url": "https://www.youtube.com/watch?v=SPGn9MZb1a0"}'

# 4. Create video from YouTube (test with a short video)
curl -X POST http://localhost:8080/api/v1/videos/youtube \
  -H "Content-Type: application/json" \
  -d '{"youtube_url": "https://www.youtube.com/watch?v=SPGn9MZb1a0", "chunk_duration": 30}'

# 5. List videos
curl http://localhost:8080/api/v1/videos

# 6. Delete video
curl -X DELETE http://localhost:8080/api/v1/videos/{video_id}} \
  -H "Content-Type: application/json"

This will:
1. Download the video
2. Create sentence-snapped chunks
3. Generate transcript
4. Return the complete video object

# 7. Check video status
curl http://localhost:8080/api/v1/videos/{video_id}

# 8. Set video downloading status:
curl -X PATCH "http://localhost:8080/api/v1/videos/{video_id}" \
  -H "Content-Type: application/json" \
  -d '{"status": "chunking_complete"}'

# 9. Retry downloading:
curl -X POST "http://localhost:8000/api/v1/videos/{video_id}/retry"

# 10. Get video chunks
curl http://localhost:8080/api/v1/videos/{video_id}/chunks

# 11. Testing Chat API with curl
   A. Test non-streaming SSE (via curl):
      curl -X POST http://localhost:8080/api/v1/chat \
        -H "Content-Type: application/json" \
        -d '{
          "messages": [
            {"role": "user", "content": "Explain the word 'speculative'."}
          ]
        }'
   B. Test with video context (video_id optional):
      curl -X POST http://localhost:8080/api/v1/chat \
        -H "Content-Type: application/json" \
        -d '{
          "video_id": "YOUR-UUID-HERE",
          "messages": [
            {"role": "user", "content": "Explain the vocabulary in this video"}
          ]
        }'
   C. Test streaming with curl's -N (no buffer) for real-time tokens:
curl -X POST http://localhost:8080/api/v1/chat \
  -H "Content-Type: application/json" \
  -H "Accept: text/event-stream" \
  -d '{
    "messages": [
      {"role": "user", "content": "Explain the word 'speculative'."}
    ]
  }' -N


# 8. Verify Storage Files
ls -la /workspaces/education/data/videos/      # Downloaded video (.webm)
ls -la /workspaces/education/data/transcripts/ # JSON transcript
ls -la /workspaces/education/data/audios/      # Temp audio (deleted after Whisper)

# 9. Test Error Handling
Submit invalid YouTube URL
curl -X POST http://localhost:8080/api/v1/videos/youtube \
  -H "Content-Type: application/json" \
  -d '{"youtube_url": "https://invalid-url.com/video"}'
Check that status is failed and error_message is set:
{"status": "failed", "error_message": "..."}

# 10. Retry failed video
curl -X POST http://localhost:8080/api/v1/videos/{video_id}/retry

# 11. Database Verification
Check video status and error_message field:
cd /workspaces/education
sqlite3 data/db/learning.db "SELECT id, title, status, error_message FROM videos;"

==========   Frontend   ==========
cd frontend && pnpm dev to start the dev server.

==========   Issues   ==========
A. The prompt is too long and there's not enough room for the full processing / response.
   1. Max_tokens=8192
   2. Transcript is explicitly truncated to first 4000 characters
   3. Video length limit: ~3-5 minutes of content per study plan

B. NVIDIA driver installation notes:
   1. Use newest NVIDIA driver.
   2. Choose appropriate version of CUDA-tools-kit based on the GPU you have.
      ex: CUDA-12.5 for Tesla P100.
   3. Install the llama-cpp-python with the corresponding cuda (tools-kit) version.
      ex: uv pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu125
C. SSE (Server-Sent Events) - A server-push technology where the server sends data to the client over HTTP, one-way communication.
   Use case for this project:
    - The single HTTP request stays open and the server pushes data chunks through it. No new connection needed.
    - Backend sends tokens one-by-one as LLM generates them
    - Frontend displays text in real-time (like ChatGPT streaming)
    - Simple to implement with FastAPI's StreamingResponse
   How it works:
   Client (Vue) ───HTTP request───▶ Backend
        ▲                        │
        │                        ▼
        └───── SSE stream ◀───── LLM generates tokens
   Alternative: WebSockets (bidirectional, more complex) - not needed since only server→client streaming is required.
   Key Design Pattern: Running a Blocking Generator in Async Context
   The challenge: llama-cpp-python's create_chat_completion with stream=True is a blocking iterator
   that must run in a thread to avoid blocking the event loop.

1. Main async code calls loop.run_in_executor to run _stream_tokens in thread pool
2. _stream_tokens is a generator that yields tokens from the LLM
3. The while loop continuously pulls tokens using _get_next (also run in executor)
4. Tokens are yielded back to the async caller
I'll create a simple ASCII/text diagram.
┌─────────────────────────────────────────────────────────────┐
│                      Async Caller                           │
└─────────────────────┬───────────────────────────────────────┘
                      │ yields token
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  while True:                                                │
│    token = await loop.run_in_executor(None, _get_next, gen) │
│    if token is None: break                                  │
│    yield token                                              │
└─────────────────────┬───────────────────────────────────────┘
                      │ run in thread pool
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  Thread Pool Executor (None = default)                      │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  _get_next(gen)         _stream_tokens()               │ │
│  │       │                        │                       │ │
│  │       │                        ▼                       │ │
│  │       │                 ┌──────────────┐               │ │
│  │       │                 │  LLM Model   │               │ │
│  │       │                 │  (blocking)  │               │ │
│  │       │                 └──────┬───────┘               │ │
│  │       │                        │                       │ │
│  │       │◄───────── yields ──────┘                       │ │
│  └───────┴────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
Flow:
1. _stream_tokens runs in thread pool, blocking on LLM
2. _get_next pulls next token from generator (also in thread pool)
3. Main loop runs in async context, yielding tokens to caller
4. When StopIteration occurs, _get_next returns None → break
